# Idea 9 技术方案:面向长程具身智能的工具对齐组合框架(免训 Chord 交接)

> 状态:方案设计 v3(2026-09-03 修订;新增 TAPT/HELM 调研对照与「规划-组合」双层框架设计,见 docs/design_optimization.md)
> 参考:ChordEdit(arXiv:2602.19083v2,已精读含附录);TAPT/Tool-Aligned VLA(arXiv:2605.13119);HELM/Harness VLA(arXiv:2604.18791)
> 定位:交叉「扩散 + 具身 + 免训 + 长程智能体」,把 ChordEdit 的低能传输场升级为**工具调用的免训交接层**,与 Idea 1 共享「最优传输场」工具,故事递进

---

## 0. 一句话总结

把 ChordEdit 的「Chord 控制场」从图像潜在空间平移到动作块空间,并把它重新定位为**长程智能体工具调用的免训交接层**:高层 VLM 按 TAPT 式接口输出调用序列 `{(g_k, z_k)}`(工具家族 g + 场景接地指令 z),低层 ChordCompose 在**冻结的工具条件扩散策略/VLA**上完成 Switch / Chain / Combine 三种免训组合;交接场能量 ‖û‖² 作为免训的风险信号驱动事件式重规划(对应 TAPT 进度反馈与 HELM 验证器)。理论部分继承 ChordEdit 能量收缩与截断误差框架,新增**工具语义条件场、工具对组合稳定性条件、可行性投影稳定性**三个理论增量;相对 ChordEdit 的新增是**工具语义条件、物理可行性投影**与**时间掩码**。

---

## 1. 要解决的问题

- 长程操作需要组合原语技能;端到端重训练昂贵,且对**未见过的技能/工具组合**泛化难。
- 现有两条路线各有短板:**训练式**(如 *SDP*、TAPT 的工具对齐后训练)需大量长程数据与适配器训练; **规划式**(TAPT 高层 VLM + HELM 验证/恢复 harness)依赖「已对齐的执行器」——TAPT 消融明确显示「只包 planner 不训执行器不可靠」,HELM 则把短板定位在验证与恢复而非生成。
- 本方案目标:在**冻结的工具条件少步扩散策略(含 VLA 骨干)**上,用**免训/少训**方式把「工具调用序列」组合成长程行为,实现 **zero-shot 工具组合**;并用场能量提供免训的「交接风险 → 重规划」信号,补齐验证与恢复短板。

---

## 2. ChordEdit 机制速览(要复用的骨架)

原文是一个清晰的四段结构,可直接平移到动作空间:

1. **条件概率流**:预训练 T2I 模型诱导 `dx/dt = v(x, t, c)`。
2. **OT 视角**:编辑 = 把源分布 `p1(x|c_src)` 传输到目标分布 `p0(x|c_tar)`,理想场 `u_t` 是 Benamou–Brenier 动态 OT 的解(最小动能 + 连续性方程)。
3. **可观测模型**:锚定到干净源图 x̄,用加噪核 `K_t(·|x̄)` 造带噪代理 z,查询模型得到残差 `R(x̄,t) = E[B_t(Q(z,t,c_tar) − Q(z,t,c_src))]`。
4. **Chord 控制场 + 近端精炼**:朴素残差场高能/发散,做**因果单边核平滑**:

```
û_t = [ t·R(x̄, t−δ) + δ·R(x̄, t) ] / (t+δ)      (Eq. 4.5, 核心公式;已按原文核实权重方向)
x_pred = x_in + λ·û                              (单次大步长传输;λ 为步长缩放)
x_tar = prox(x_pred, tc, c_tar)                  (可选近端精炼)
```

关键超参(原文默认值,动作域需重新标定):主查询时刻 t=0.90、平滑窗口 δ=0.15、
步长缩放 λ、精炼时刻 tc=0.30、噪声样本 n=1(原文证明 n>1 边际收益可忽略)。

理论骨架(附录 D/E,可整体迁移):

| 结论 | 出处 | 含义 |
|---|---|---|
| L² 能量收缩 `‖û‖ ≤ ‖R‖` | Prop D.1 / Cor D.3 | Jensen → 压制高能尖峰 |
| 局部截断误差更小 | Lemma D.4 + Prop D.5 | Chord 场比朴素差场更平滑 |
| 全局 O(h) 收敛且误差常数更小 | Thm D.6 | 单步(h=1)可行 |
| 单步误差 + 稳定性条件 | Thm E.6 | 何时单步安全(C_cho ≤ C_nai) |
| 与 Benamou–Brenier 最优能量的间隙 | Thm E.4 | 逼近最优传输的程度(O(δ) 间隙) |
| 可观测残差映射 B_t 闭式系数 | 附录 C | 噪声预测 A_t^(ε)=−α̇/(ασ)=β/(2σ);x0 预测 A_t^(x0)=α̇/σ²;v 预测 A_t^(v)=−α̇/σ;流匹配 B_t≡I |

**核心洞察**:ChordEdit 用「时间加权平均」把「两个大向量之差」这种高能发散场,变成 L² 收缩的低能场,从而让**单次大步长积分**可行。Idea 9 把这个算子搬到动作空间,解决「技能切换/组合」的高能发散问题。

---

## 3. 问题形式化:工具调用组合 = 传输问题

**设定**:冻结的、蒸馏到少步(1~4 步)的**工具条件扩散策略/VLA**,在动作块空间上诱导条件概率流:

```
da/dt = v(a, t, o, c)      # a ∈ R^{H×da} 动作块,o 观测,c = (g, z) 工具调用
g ∈ G                     # 离散工具家族:grasp / open / close / place / rotate / ...
z ∈ Z                     # 场景接地指令(语言):对象、空间关系、期望局部效果
e(g, z)                   # 调用嵌入 = [one-hot(g); enc(z)],enc 为冻结文本编码器
```

工具家族 `G = {g1, ..., gM}`,每个调用 `c=(g,z)` 定义动作分布 `p(a|o,c)`(TAPT 式
「有界调用」语义:一个工具执行一段有界窗口,而非完整任务)。长程任务 = 高层智能体
产出的调用序列 `{(g_k, z_k)}_{k=1..K}`。

### 3.1 核心映射表

| ChordEdit(图像编辑) | Idea 9(工具调用组合) |
|---|---|
| 源/目标提示 c_src / c_tar | 当前调用 (g,z) / 目标调用 (g′,z′) |
| 干净源图 x1(锚点) | 当前动作块 a(锚点) |
| 正向加噪核 K_t(·\|x̄) | DP 原生的动作空间正向加噪核 |
| 模型输出 Q(z,t,c) | 工具条件策略输出 Q(a,t,o,c) |
| 线性映射 B_t(附录 C) | 动作空间同构的 B_t |
| 残差场 R = E[B_t(Q_tar−Q_src)] | R = E[B_t(Q_{c′}−Q_c)],c=(g,z), c′=(g′,z′) |
| Chord 场 û(式 4.5) | 同式 |
| 单步 x_pred = x_in + λ·û | a_pred = a + λ·û(λ 步长缩放) |
| 近端精炼(语义增强) | 可行性投影(运动学/动力学) |
| 背景一致性 PSNR | 轨迹平滑度 / 未修改段保真 |
| CLIP-Edited 语义对齐 | 调用忠实度(Faithful / Non-biased Rate,TAPT 式) |

### 3.2 三个原子算子(由语言调用驱动)

- **Switch(切换)**:执行中从调用 (g,z) 换成 (g′,z′),保留轨迹既有部分 —— 直接对应「编辑」。
- **Chain(串联)**:长程任务 = 调用序列 c1→…→cK,由多次 Switch + 交接耦合构成。
- **Combine(叠加)**:同时满足多个调用约束(乘积专家),如「grasp(A) ∧ 保持朝向」。

### 3.3 双层框架「规划-组合」(Plan-and-Compose,新增)

```
高层(免训 VLM 智能体, TAPT 式接口):
  o_t → 场景分析 → 调用序列 {(g_k, z_k)};收到风险信号 ‖û‖² 超标时事件式重规划
低层(冻结工具条件策略 + ChordCompose):
  每个调用 c_k 执行有界窗口;边界处 Chord 场完成 c_k → c_{k+1} 免训交接;
  可行性投影做执行前验证(HELM 验证器的免训替代);
  反馈给高层:进度(调用内状态) + 交接风险(场能量) + 投影残差
```

关键叙事:**TAPT 证明「执行器必须对齐成可靠工具」,本文证明「未对齐的冻结执行器也能被
Chord 场可靠调用」——免训对齐层**;HELM 把短板定位在验证/恢复,本文用投影+场能量
提供免训的验证与恢复触发信号。

---

## 4. 核心机制:工具语义条件 Chord 组合场

### 4.0 工具语义条件(新增)

调用嵌入 `e(g,z) = [one-hot(g); enc(z)]`,enc 为**冻结**文本编码器(VLA 骨干自带或 CLIP)。
条件场查询 `Q(a,t,o,e(g,z))`。两个调用之间的残差场把「工具家族差异」与「接地指令差异」
同时纳入交接语义:

```
ΔQ(z,t) = Q(z,t,o,e(g′,z′)) − Q(z,t,o,e(g,z))   # 同工具换指令 / 跨工具换家族均适用
```

与 TAPT 的对照:其工具家族由**训练出的残差适配器 ΔW_g**实现(参数 +9%),本文在同一冻结
骨干上用**推理期 Chord 场**切换工具路径(参数 +0),且带稳定性理论。

### 4.1 Switch —— 工具切换场(编辑的直译)

朴素做法是硬切:策略从 `v(a,t,o,c)` 换成 `v(a,t,o,c′)`。在少步蒸馏策略里,条件→向量场是高度非线性的敏感映射,硬切产生高能发散场,动作块抖动/不可执行 —— 即 ChordEdit Figure 3/4 的失败模式。

Chord 方案:

```
R_{c→c′}(a, t)   = E_{z~K_t}[ B_t( Q(z,t,o,e(c′)) − Q(z,t,o,e(c)) ) ]
R_{c→c′}(a, t−δ) = E_{z~K_{t−δ}}[ B_{t−δ}( Q(z,t−δ,o,e(c′)) − Q(z,t−δ,o,e(c)) ) ]
û = [ t·R(t−δ) + δ·R(t) ] / (t+δ)        # 更早时刻 t−δ 的测量权重为 t(原文权重方向)
a ← a + λ·û                              # λ:步长缩放
```

配套**时间掩码**(对应 ChordEdit 的「非编辑区域保持」):只在切换边界附近的时间步施加 û,其余时间步不动,保住正在执行的动作段。

### 4.2 Chain —— 长程串联 + 交接耦合

每个边界 k 把「调用 c_k 末端的动作分布」搬到「调用 c_{k+1} 初始的动作分布」。这是 OT 的用武之地:**最优交接 = 两个调用动作分布之间的最小能量耦合**。每次交接用一次 Chord 单步传输完成,全程免训。

理论上最有故事:Chord 场是 Benamou–Brenier 最优传输场 `u_t` 的低能估计器,所以「工具交接」被严格定义成「最优传输耦合」,而非经验性硬拼接。

### 4.3 Combine —— 并发叠加(乘积专家)

同时满足多个调用约束(如「接近物体」∧「保持朝向」):组合分布 `p ∝ Π_i p_i^{w_i}`,漂移线性叠加 `v_combine = Σ_i w_i v_i`(归一化)。naive 求和同样高能,用 Chord 平滑:

```
R_combine(a, t) = E[ B_t( v_combine(z,t,o) − v_base(z,t,o) ) ]
û = [ t·R(t−δ) + δ·R(t) ] / (t+δ)
```

### 4.4 算法伪代码(Plan-and-Compose)

```
Algorithm: Plan-and-Compose(高层规划 + 低层 Chord 交接)
输入: 观测流 o, 高层调用序列 {(g_k, z_k)}, 权重 w, 超参 t, δ, λ, tc, 风险阈值 ρ
输出: 长程动作执行
1. (高层) VLM: o → 调用序列 {(g_k, z_k)};计算调用嵌入 e_k = [one-hot(g_k); enc(z_k)]
2. 低层循环 k = 1..K:
     a ← 以 e_k 条件采样/续用当前动作块
     执行 a 的有界窗口(receding-horizon)
     if k < K:                         # 交接
       R(t), R(t−δ) ← 查询 c_k → c_{k+1} 的语义残差场(共享噪声)
       û ← [ t·R(t−δ) + δ·R(t) ] / (t+δ)
       if ‖û‖² > ρ: 高层事件式重规划(风险触发,对应 TAPT 进度反馈/HELM 验证器)
       a ← a + λ·û · mask_k            # 单步传输 + 时间掩码
       a ← prox_feasible(a, tc, o)     # 执行前可行性验证(非扩张投影)
3. (可选) 多约束处把 v 换为 Σ w_i v_i 后用同法平滑(Combine)
```

---

## 5. 理论部分:直接迁移 + 三个新定理

ChordEdit 附录 D/E 的定理把状态空间从图像潜在换成动作块后**逐条成立**(只依赖 L² 收缩、Lipschitz、Jensen,不依赖具体空间)。

可直接继承:

- **组合场能量收缩**(Prop D.1/Cor D.3):`‖û‖ ≤ ‖R‖` → 切换后动作块 jerk 更低、轨迹更平滑。
- **单步组合误差界**(Lemma D.4 + Thm E.6):h=1 单步下 Chord 组合截断误差 `M_f^chord ≤ M_f^naive`。

### 新增理论贡献(区别于照搬 ChordEdit;按稳回程度排序)

1. **可行性投影不破坏稳定性(头号新增定理)**:设可行性约束集 C ⊂ R^{H×da} 为凸集(动作界/关节速度限/工作空间均为凸约束),投影 P_C 是 firmly non-expansive(1-Lipschitz 且复合后 Jacobian 谱范数不增)。由 Euler 单步映射 `a ↦ P_C(a + λ·û(a,t))` 的 Jacobian 有界性,可导出投影后的显式 Euler 稳定性条件不劣于未投影情形:**投影只收缩不放大误差**。这是 ChordEdit 没有的(图像无物理约束),且有真实数学内容。

2. **工具对组合稳定性条件(核心卖点,需实验闭环)**:Thm E.6 只给出 C_cho ≤ C_nai,其稳定性条件 h·L<1 与「哪两个工具可以组合」无关。本文把它升级为调用对 (c,c′) 的可组合判据:把局部 Lipschitz 常数分解为 `L(c,c′) = L(g) + c·‖e(g,z) − e(g′,z′)‖`(语义距离上界化场差异,同一家族 g 内只差接地指令项),导出充分条件:

```
λ·( L(g) + c·‖e(g,z) − e(g′,z′)‖ ) < 1  ⇒  单步交接误差 ≤ ε(c,c′)
```

**直接回答「哪两个工具调用可以免训交接、误差多大」**,把免训组合从启发式变成有判据的方法。实验中沿交接轨迹用有限差分估计 ‖∇_a v‖ 验证(理论-实验闭环);语义距离可由嵌入对直接计算,构成可预测的「可组合性图」(composability graph)。风险预案:若实验不支持硬判据,降级为「经验稳定性代理指标」,不写成定理。

3. **组合最优性(推论,无新证明)**:迁移 Thm E.4,Chord 组合场能量与 Benamou–Brenier 最优交接能量的间隙为 `O(δ)` —— δ 的 bias-variance 折中,证明组合逼近 OT 最优交接,给出最优 δ 的选择方向。

---

## 6. 与图像编辑不同的四个关键难点(及对策)

1. **锚点**:图像编辑有「干净源图 x1」当锚点;DP 里**动作块本身就是锚点**(当前要执行的轨迹),加噪核用 DP 原生正向过程。需要把附录 C 的 `B_t` 闭式系数(VP 调度)改写到动作空间 —— 已核实为纯代数:
   - DP 若用噪声预测参数化:`A_t^(ε) = −α̇(t)/(α(t)σ(t)) = β(t)/(2σ(t))`
   - DP 若用 x0 预测参数化:`A_t^(x0) = α̇(t)/σ(t)²`
   - DP 若用流匹配/速度参数化:`B_t ≡ I`(最简)
   注意实现细节:锚点可以是任意噪声水平的「当前动作块 + 其干净估计」,查询时刻 t 作为超参消融。

2. **条件空间离散化 → 工具调用语义化**:文本提示是连续语义差,技能是离散 token,工具调用是「离散家族 + 连续指令」混合。对策:调用嵌入 `e(g,z)=[one-hot(g); enc(z)]`(enc 冻结),「调用差」= 嵌入差,连续传输仍成立;单 one-hot 退化为特例(纯家族切换)。

3. **物理可行性**:图像无动力学约束,动作有。→ 新增 `prox_feasible` 投影(运动学/动力学/碰撞),是本工作相对 ChordEdit 的**新增模块**,也是论文独立性的来源之一。

4. **「非编辑区域保持」→「轨迹段保持」**:用时间掩码实现,只传输交接边界附近的时间步。

---

## 7. 验证方案(v3:对标 TAPT/HELM 的长程评测体系)

- **基础模型(两档)**:
  1. 状态空间技能条件 DP(已跑通,机制验证与全量消融);
  2. **VLA 骨干档**:冻结预训练 VLA(OpenVLA-OFT / Octo / π0.5)的视觉-语言特征作为观测编码,
     或直接以 VLA 为工具执行器(冻结 + 少步采样),验证「冻结 VLA + Chord 交接」路径;
     一致性蒸馏(修复 c_skip/c_out 参数化)得到 1/2/4 步工具。
- **工具库**:工具家族 G = {grasp, open, close, place, rotate, push};调用 = (g, z),
  z 为场景接地指令(LIBERO 子目标语言 / BDDL 目标谓词);条件嵌入 = [one-hot(g); enc(z)]。
- **基准(三阶段)**:
  1. **Meta-World 状态空间(机制验证)**:5 原语技能链与 unseen 组合,已跑通(结论见 docs/experiment_report.md)。
  2. **LIBERO-Long 在线评测(主战场)**:BDDL 成功判定;对比 naive / chord / eff_shift / energy / e2e-trained / **TAPT 式训练对齐(IA 单元 + 残差适配器)**——直接对标 TAPT 的 97.2 与 HELM 的 +23.1。
  3. **LIBERO-CF-Long 反事实评测(调用忠实度)**:改目标对象/空间关系/截断指令的反事实任务,报告 **Faithful Rate / Non-biased Rate**(TAPT 指标体系),验证「Chord 交接让未对齐冻结执行器可靠调用」的分水岭主张。
- **恢复评测(对标 HELM)**:注入扰动(掉落/碰撞),场能量 ‖û‖² 上升 → 事件式重规划的成功率(对标其 54.2% 恢复率)。
- **Baseline**:(a) naive 硬切、(b) TAPT 式训练对齐、(c) 端到端多任务、(d) GSC 式能量组合、(e) 有效时间步平移、(f) planner-only(高层序列 + 未对齐执行器,即 TAPT 消融中最弱配置)。
- **指标**:成功率、轨迹能量/平滑度、调用忠实度(Faithful/Non-biased Rate)、NFE/延迟、OOS、交接处 jerk、**重规划触发率与恢复成功率**。
- **消融**:t、δ、λ、n、±可行性投影、±时间掩码、Chord vs 时间步平移、one-hot vs 语义嵌入条件、±场能量重规划。
- **理论-实验一致性**:稳定性条件(定理 2)在**工具对层面**的闭环:语义距离 ‖e(g,z)−e(g′,z′)‖ vs 实测 Lipschitz/成功率,构造可组合性图并验证预测力。

---

## 8. 风险与未决问题

1. **前置依赖**:VLA 骨干档需要预训练权重与图像观测管线(本地有 LIBERO 图像数据与 OpenVLA 生态);一致性蒸馏有开源实现但需修复参数化(见实验报告),先用 DDIM 少步/状态空间跑通机制。
2. **低能=可行这个对应关系**在动作空间是否成立,已有方向性实验证据(chord > naive 3×、能量 3×↓),但「场能量 → 重规划触发」的闭环仍需恢复评测验证。
3. **审稿人必问「为什么不训练」**:TAPT 消融显示「planner 包裹不训执行器不可靠」——本文的分水岭实验必须是「Chord 交接让未对齐冻结执行器达到接近训练对齐的忠实度」,若做不出则 novelty 大减;备选定位:Chord 作为 TAPT 的免训补充(少训)。
4. **ChordEdit 复现论文的「时间步平移」质疑**(2026-08,arXiv):必须做「Chord 平滑 vs 有效时间步平移」消融正面回应,论证 δ>0 平滑在动作域的独立价值。
5. **技能组合邻域非空白**:GSC/SCaR/SkillDiffuser/BOSS/DeCo/TAPT/HELM 均为近距离工作。本文的差异化=「冻结少步策略上的免训工具交接 + 语义稳定性判据 + 免训验证/恢复信号」,baseline 必须包含训练式组合(TAPT 式)与验证/恢复(HELM 式)。
6. **VLM 高层规划器依赖**:若真实 VLM 接口不可用,先用脚本化 oracle planner(阶段图/子目标语言)验证闭环,真 VLM 作为可替换组件。
7. 已核实:ChordEdit 原文只做了 **text-guided editing**,**未覆盖可控生成与动作域**,所以 Idea 1 的「控制场」与本文「动作域工具交接场」仍是空白,不撞车。

---

## 9. 与 Idea 1 的关系

同一套 OT 场工具,两个场景:

- Idea 1:场作用在**图像潜在空间**,约束是视觉条件(深度图/布局)。
- Idea 9:场作用在**动作/轨迹空间**,约束是技能序列的衔接与叠加。

推荐先在图像域(数据便宜、迭代快)打磨场的构造与稳定性理论,再迁移到机器人域,两篇论文共享一套工具和理论,故事递进。

---

## 10. 调研与修订记录

- 完整调研报告见 [docs/survey.md](docs/survey.md)(2026-09-02):25 篇文献、逐节对照、novelty 增量定位、撞车分析。
- 概念验证实验结论(2026-09-02)见 [docs/experiment_report.md](docs/experiment_report.md):
  Chord 场 vs 硬切换成功率 3×、场能量 3×↓;可行性投影为最强独立贡献(LIBERO 拼接 MSE -36%)。
- v2 修订要点:Chord 场公式权重按原文修正(`û = [t·R(t−δ) + δ·R(t)]/(t+δ)`)、τ→λ 符号分离、B_t 动作空间闭式系数补全、新增定理重排(投影稳定性为头号)、验证方案落地为「Meta-World 主 + LIBERO 增信」、风险清单更新(时间步平移质疑/GSC-SCaR 撞车)。
- 第三轮审计(2026-09-03)见 [docs/experiment_report.md](docs/experiment_report.md) §9:
  **B_t 符号 bug 已修复**(A_t^(eps) = −γ_max/(2σ), 修复后 5 链 0.25→0.64);
  新增 chord_recon 方法(Chord 平滑 + 一步 x0 重建, 24 回合 0.708)。
- 第四轮(2026-09-03)见 [docs/experiment_report.md](docs/experiment_report.md) §10 与
  [docs/theory_composability.md](docs/theory_composability.md):
  L(c,c′) 分解定理严格化(家族距离+接地距离, 链式误差可加分解);
  1 步学生无差距的根因诊断(场 τ-共线 0.98 → 平滑退化, 传输编辑本身有效 -63% err);
  图像 CNN-DP 试点(同任务对照: 状态 0.42 / 无增广图像 0.25 / **增广+60k 图像 0.62**;
  从零 CNN 是杠杆, 但必须配平移增广+足够迭代, 已扩至全 10 任务)。
- v3 修订要点(2026-09-03,见 [docs/design_optimization.md](docs/design_optimization.md)):调研 TAPT(arXiv:2605.13119)与 HELM(arXiv:2604.18791),方案升级为「规划-组合」双层框架;条件从 one-hot 技能升级为工具调用 (g,z) 语义嵌入;新增场能量风险信号与事件式重规划;定理 2 语义化为工具对组合稳定性条件;验证方案对标 LIBERO-Long 在线/CF-Long 忠实度/HELM 式恢复评测。
