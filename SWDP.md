# Idea 9 技术方案:机器人扩散策略的推理期技能组合(免训)

> 状态:方案设计 v2(2026-09-02 修订;依据 docs/survey.md 调研报告与 ChordEdit 原文逐条核实)
> 参考:ChordEdit: One-Step Low-Energy Transport for Image Editing(arXiv:2602.19083v2,已精读含附录)
> 定位:交叉「扩散 + 具身 + 免训」三个方向,与 Idea 1 共享「最优传输场」工具,故事递进

---

## 0. 一句话总结

把 ChordEdit 的「Chord 控制场」从图像潜在空间平移到动作块空间,用它实现技能**切换 / 串联 / 叠加**三种免训组合;理论部分继承其能量收缩与截断误差框架,并新增「零样本组合稳定性条件」这一核心定理;相对 ChordEdit 的新增是**物理可行性投影**与**时间掩码**。

---

## 1. 要解决的问题

- 长程操作需要组合原语技能;端到端重训练昂贵,且对**未见过的技能组合**泛化难。
- 现有技能组合多靠训练(如 *Learning Diffusion Policy from Primitive Skills*)。
- 目标是:在**冻结的少步扩散策略**上,用**免训/少训**方式组合原语技能,实现 **zero-shot 技能组合**。

---

## 2. ChordEdit 机制速览(要复用的骨架)

原文是一个清晰的四段结构,可直接平移到动作空间:

1. **条件概率流**:预训练 T2I 模型诱导 `dx/dt = v(x, t, c)`。
2. **OT 视角**:编辑 = 把源分布 `p1(x|c_src)` 传输到目标分布 `p0(x|c_tar)`,理想场 `u_t` 是 Benamou–Brenier 动态 OT 的解(最小动能 + 连续性方程)。
3. **可观测模型**:锚定到干净源图 x̄,用加噪核 `K_t(·|x̄)` 造带噪代理 z,查询模型得到残差 `R(x̄,t) = E[B_t(Q(z,t,c_tar) − Q(z,t,c_src))]`。
4. **Chord 控制场 + 近端精炼**:朴素残差场高能/发散,做**因果单边核平滑**:

```
û_t = [ t·R(x̄, t−δ) + δ·R(x̄, t) ] / (t+δ)      (Eq. 4.5, 核心公式;已按原文核实权重方向)
x_pred = x_in + λ·û                              (单次大步长传输;λ 为步长缩放)
x_tar = prox(x_pred, tc, c_tar)                  (可选近端精炼)
```

关键超参(原文默认值,动作域需重新标定):主查询时刻 t=0.90、平滑窗口 δ=0.15、
步长缩放 λ、精炼时刻 tc=0.30、噪声样本 n=1(原文证明 n>1 边际收益可忽略)。

理论骨架(附录 D/E,可整体迁移):

| 结论 | 出处 | 含义 |
|---|---|---|
| L² 能量收缩 `‖û‖ ≤ ‖R‖` | Prop D.1 / Cor D.3 | Jensen → 压制高能尖峰 |
| 局部截断误差更小 | Lemma D.4 + Prop D.5 | Chord 场比朴素差场更平滑 |
| 全局 O(h) 收敛且误差常数更小 | Thm D.6 | 单步(h=1)可行 |
| 单步误差 + 稳定性条件 | Thm E.6 | 何时单步安全(C_cho ≤ C_nai) |
| 与 Benamou–Brenier 最优能量的间隙 | Thm E.4 | 逼近最优传输的程度(O(δ) 间隙) |
| 可观测残差映射 B_t 闭式系数 | 附录 C | 噪声预测 A_t^(ε)=−α̇/(ασ)=β/(2σ);x0 预测 A_t^(x0)=α̇/σ²;v 预测 A_t^(v)=−α̇/σ;流匹配 B_t≡I |

**核心洞察**:ChordEdit 用「时间加权平均」把「两个大向量之差」这种高能发散场,变成 L² 收缩的低能场,从而让**单次大步长积分**可行。Idea 9 把这个算子搬到动作空间,解决「技能切换/组合」的高能发散问题。

---

## 3. 问题形式化:技能组合 = 传输问题

**设定**:冻结的、蒸馏到少步(1~4 步)的**技能条件扩散策略**,在动作块空间上诱导条件概率流:

```
da/dt = v(a, t, o, s)      # a ∈ R^{H×da} 动作块,o 观测,s 技能
```

技能库 `S = {s1, ..., sM}`。每个技能 s 定义一个动作分布 `p(a|o,s)`。

### 3.1 核心映射表

| ChordEdit(图像编辑) | Idea 9(技能组合) |
|---|---|
| 源/目标提示 c_src / c_tar | 当前技能 s / 目标技能 s′ |
| 干净源图 x1(锚点) | 当前动作块 a(锚点) |
| 正向加噪核 K_t(·\|x̄) | DP 原生的动作空间正向加噪核 |
| 模型输出 Q(z,t,c) | 技能条件策略输出 Q(a,t,o,s) |
| 线性映射 B_t(附录 C) | 动作空间同构的 B_t |
| 残差场 R = E[B_t(Q_tar−Q_src)] | R = E[B_t(Q_{s′}−Q_s)] |
| Chord 场 û(式 4.5) | 同式 |
| 单步 x_pred = x_in + τ·û | a_pred = a + λ·û(λ 步长缩放) |
| 近端精炼(语义增强) | 可行性投影(运动学/动力学) |
| 背景一致性 PSNR | 轨迹平滑度 / 未修改段保真 |
| CLIP-Edited 语义对齐 | 技能约束满足率 |

### 3.2 三个原子算子

- **Switch(切换)**:执行中从技能 s 换成 s′,保留轨迹既有部分 —— 直接对应「编辑」。
- **Chain(串联)**:长程任务 = 技能序列 s1→…→sK,由多次 Switch + 交接耦合构成。
- **Combine(叠加)**:同时满足多个技能约束(乘积专家)。

---

## 4. 核心机制:Chord 组合场

### 4.1 Switch —— 技能切换场(编辑的直译)

朴素做法是硬切:策略从 `v(a,t,o,s)` 换成 `v(a,t,o,s′)`。在少步蒸馏策略里,条件→向量场是高度非线性的敏感映射,硬切产生高能发散场,动作块抖动/不可执行 —— 即 ChordEdit Figure 3/4 的失败模式。

Chord 方案:

```
R_{s→s′}(a, t)   = E_{z~K_t}[ B_t( Q(z,t,o,s′) − Q(z,t,o,s) ) ]
R_{s→s′}(a, t−δ) = E_{z~K_{t−δ}}[ B_{t−δ}( Q(z,t−δ,o,s′) − Q(z,t−δ,o,s) ) ]
û = [ t·R(t−δ) + δ·R(t) ] / (t+δ)        # 更早时刻 t−δ 的测量权重为 t(原文权重方向)
a ← a + λ·û                              # λ:步长缩放
```

配套**时间掩码**(对应 ChordEdit 的「非编辑区域保持」):只在切换边界附近的时间步施加 û,其余时间步不动,保住正在执行的动作段。

### 4.2 Chain —— 长程串联 + 交接耦合

每个边界 k 把「技能 s_k 末端的动作分布」搬到「技能 s_{k+1} 初始的动作分布」。这是 OT 的用武之地:**最优交接 = 两个技能动作分布之间的最小能量耦合**。每次交接用一次 Chord 单步传输完成,全程免训。

理论上最有故事:Chord 场是 Benamou–Brenier 最优传输场 `u_t` 的低能估计器,所以「技能交接」被严格定义成「最优传输耦合」,而非经验性硬拼接。

### 4.3 Combine —— 并发叠加(乘积专家)

同时满足多个技能(如「接近物体」∧「保持朝向」):组合分布 `p ∝ Π_i p_i^{w_i}`,漂移线性叠加 `v_combine = Σ_i w_i v_i`(归一化)。naive 求和同样高能,用 Chord 平滑:

```
R_combine(a, t) = E[ B_t( v_combine(z,t,o) − v_base(z,t,o) ) ]
û = [ t·R(t−δ) + δ·R(t) ] / (t+δ)
```

### 4.4 算法伪代码

```
Algorithm: ChordCompose
输入: 观测 o, 技能序列 (s1..sK), 权重 w, 超参 t, δ, λ, tc
输出: 长程动作块 a
1. a ← 采样噪声(或当前正在执行的块)
2. for k = 1..K−1:                      # 每个交接
     R(t), R(t−δ) ← 查询技能 s_k → s_{k+1} 的残差场(共享噪声)
     û ← [ t·R(t−δ) + δ·R(t) ] / (t+δ)
     a ← a + λ·û · mask_k              # 单步传输 + 时间掩码
3. (可选) 需要多约束处,把 v 换为 Σ w_i v_i 后用同法平滑
4. (可选) a ← prox_feasible(a, tc, o)  # 单步可行性投影
5. 执行 a 的前 k 步,receding-horizon 重复
```

---

## 5. 理论部分:直接迁移 + 三个新定理

ChordEdit 附录 D/E 的定理把状态空间从图像潜在换成动作块后**逐条成立**(只依赖 L² 收缩、Lipschitz、Jensen,不依赖具体空间)。

可直接继承:

- **组合场能量收缩**(Prop D.1/Cor D.3):`‖û‖ ≤ ‖R‖` → 切换后动作块 jerk 更低、轨迹更平滑。
- **单步组合误差界**(Lemma D.4 + Thm E.6):h=1 单步下 Chord 组合截断误差 `M_f^chord ≤ M_f^naive`。

### 新增理论贡献(区别于照搬 ChordEdit;按稳回程度排序)

1. **可行性投影不破坏稳定性(头号新增定理)**:设可行性约束集 C ⊂ R^{H×da} 为凸集(动作界/关节速度限/工作空间均为凸约束),投影 P_C 是 firmly non-expansive(1-Lipschitz 且复合后 Jacobian 谱范数不增)。由 Euler 单步映射 `a ↦ P_C(a + λ·û(a,t))` 的 Jacobian 有界性,可导出投影后的显式 Euler 稳定性条件不劣于未投影情形:**投影只收缩不放大误差**。这是 ChordEdit 没有的(图像无物理约束),且有真实数学内容。

2. **零样本组合的稳定性条件(核心卖点,需实验闭环)**:Thm E.6 只给出 C_cho ≤ C_nai,其稳定性条件 h·L<1 与「哪两个技能可组合」无关。本文把它升级为技能对 (s,s′) 的可组合判据:把局部 Lipschitz 常数分解为 `L(s,s′) = L(s) + c·‖e_s − e_s′‖`(e_s 为技能嵌入,第二项由嵌入距离上界化场差异),导出充分条件:

```
λ·( L(s) + c·‖e_s − e_s′‖ ) < 1  ⇒  单步交接误差 ≤ ε(s,s′)
```

**直接回答「哪两个技能可以免训组合、误差多大」**,把免训组合从启发式变成有判据的方法。实验中沿交接轨迹用有限差分估计 ‖∇_a v‖ 验证(理论-实验闭环)。风险预案:若实验不支持硬判据,降级为「经验稳定性代理指标」,不写成定理。

3. **组合最优性(推论,无新证明)**:迁移 Thm E.4,Chord 组合场能量与 Benamou–Brenier 最优交接能量的间隙为 `O(δ)` —— δ 的 bias-variance 折中,证明组合逼近 OT 最优交接,给出最优 δ 的选择方向。

---

## 6. 与图像编辑不同的四个关键难点(及对策)

1. **锚点**:图像编辑有「干净源图 x1」当锚点;DP 里**动作块本身就是锚点**(当前要执行的轨迹),加噪核用 DP 原生正向过程。需要把附录 C 的 `B_t` 闭式系数(VP 调度)改写到动作空间 —— 已核实为纯代数:
   - DP 若用噪声预测参数化:`A_t^(ε) = −α̇(t)/(α(t)σ(t)) = β(t)/(2σ(t))`
   - DP 若用 x0 预测参数化:`A_t^(x0) = α̇(t)/σ(t)²`
   - DP 若用流匹配/速度参数化:`B_t ≡ I`(最简)
   注意实现细节:锚点可以是任意噪声水平的「当前动作块 + 其干净估计」,查询时刻 t 作为超参消融。

2. **条件空间离散化**:文本提示是连续语义差,技能是离散 token。对策:技能用**可插值嵌入**表示,「技能差」= 嵌入差,连续传输仍成立;VLA 场景直接用自然语言技能描述。

3. **物理可行性**:图像无动力学约束,动作有。→ 新增 `prox_feasible` 投影(运动学/动力学/碰撞),是本工作相对 ChordEdit 的**新增模块**,也是论文独立性的来源之一。

4. **「非编辑区域保持」→「轨迹段保持」**:用时间掩码实现,只传输交接边界附近的时间步。

---

## 7. 验证方案(已落地修订版)

- **基础模型**:先标准 Diffusion Policy 做概念验证(证明组合场有效),再上一致性蒸馏(Consistency Policy 路线)得到 1/2/4 步 DP 证明「单步价值」(对应 ChordEdit 的「在快速模型上才有意义」)。
- **技能载体**:自训技能条件 DP(SDP 风格:技能 one-hot/嵌入条件;Meta-World 用 one-hot,LIBERO 用子目标语言/原子动作标签)。
- **基准**(两阶段):
  1. **Meta-World ML10(状态空间,主基准)**:技能 = reach/pick-place/push/door-open/drawer-close/window-open/sweep/…;任务 = 长程序列 + 重点测 **unseen 组合的 zero-shot 成功率**;训练分钟级,适合全量消融。
  2. **LIBERO-10(增信基准)**:复用本地 153GB 数据集与 turbovla-libero 环境,与 SDP(训练式组合 baseline 的载体论文)同域对齐,复证核心结论。
- **Baseline**:(a) 硬切换(naive,等价 δ=0)、(b) 训练式组合(SCaR 式交接正则 / SDP 式 router)、(c) 端到端多任务策略、(d) GSC 式能量组合拼接、(e) 有效时间步平移(只用 t−δ 时刻查询,呼应 ChordEdit 复现论文的质疑)。
- **指标**:成功率、轨迹能量/平滑度(类比 PSNR「背景一致性」)、技能约束满足率(类比 CLIP-Edited)、NFE/延迟、**OOS(观测空间偏移,参考 BOSS 论文)**、交接处 jerk。
- **消融**:t、δ、λ、噪声样本 n、有无可行性投影、有无时间掩码、Chord 平滑 vs 有效时间步平移。
- **理论-实验一致性**:验证稳定性条件(新增定理 2)能否预测哪些技能对组合成功 —— 审稿人最看重的闭环;同时验证投影后稳定性(新增定理 1)。

---

## 8. 风险与未决问题

1. **前置依赖**:需要少步蒸馏的扩散策略做「单步价值」论证;一致性蒸馏有开源实现(Consistency Policy),风险可控;先用标准 DP 跑通机制,蒸馏作为二阶段。
2. **低能=可行这个对应关系**在动作空间是否成立,需要实验确认 —— 动作空间维度低、结构强,行为可能和图像不同(但这也正好是论文的讨论点)。按 ChordEdit 复现论文的情报预期:Chord 场主要改善「轨迹平滑/交接保真」,「目标技能达成」需配合可行性投影与后续推理,指标分开报。
3. **审稿人必问「为什么不训练」**:每个消融都要用「训练成本 vs 免训收益」兜底,SCaR(训练式交接正则)是对照物。
4. **ChordEdit 复现论文的「时间步平移」质疑**(2026-08,arXiv):其发现 Chord 场主要由 t−δ 时刻主导。必须做「Chord 平滑 vs 有效时间步平移」消融正面回应,论证 δ>0 平滑在动作域的独立价值(方差降低、与时间掩码兼容)。
5. **技能组合邻域非空白**:GSC(CoRL'23)/SCaR(NeurIPS'24)/SkillDiffuser(ICLR'24)/BOSS/DeCo 均为近距离工作。本文的差异化=「冻结少步策略上的免训组合 + 稳定性判据 + 物理可行性投影」三重差异,且 baseline 必须包含训练式组合证明免训路线在 unseen 组合上不输。
6. 已核实:ChordEdit 原文只做了 **text-guided editing**,**未覆盖可控生成与动作域**,所以 Idea 1 的「控制场」与本文「动作域组合场」仍是空白,不撞车。

---

## 9. 与 Idea 1 的关系

同一套 OT 场工具,两个场景:

- Idea 1:场作用在**图像潜在空间**,约束是视觉条件(深度图/布局)。
- Idea 9:场作用在**动作/轨迹空间**,约束是技能序列的衔接与叠加。

推荐先在图像域(数据便宜、迭代快)打磨场的构造与稳定性理论,再迁移到机器人域,两篇论文共享一套工具和理论,故事递进。

---

## 10. 调研与修订记录

- 完整调研报告见 [docs/survey.md](docs/survey.md)(2026-09-02):25 篇文献、逐节对照、novelty 增量定位、撞车分析。
- 概念验证实验结论(2026-09-02)见 [docs/experiment_report.md](docs/experiment_report.md):
  Chord 场 vs 硬切换成功率 3×、场能量 3×↓;可行性投影为最强独立贡献(LIBERO 拼接 MSE -36%)。
- v2 修订要点:Chord 场公式权重按原文修正(`û = [t·R(t−δ) + δ·R(t)]/(t+δ)`)、τ→λ 符号分离、B_t 动作空间闭式系数补全、新增定理重排(投影稳定性为头号)、验证方案落地为「Meta-World 主 + LIBERO 增信」、风险清单更新(时间步平移质疑/GSC-SCaR 撞车)。
